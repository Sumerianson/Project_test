<nav class="navbar navbar-expand-lg bg-body-text-info-emphasis">
    <div class="container-fluid">
      <a class="navbar-brand" href="{{ path ('app_home') }}">Hôpital</a>
      <button class="navbar-toggler" type="button" data-bs-toggle="collapse" data-bs-target="#navbarSupportedContent" aria-controls="navbarSupportedContent" aria-expanded="false" aria-label="Toggle navigation">
        <span class="navbar-toggler-icon"></span>
      </button>
      <div class="collapse navbar-collapse" id="navbarSupportedContent">
        <ul class="navbar-nav me-auto mb-2 mb-lg-0">
        {% if app.user %}
  
          <li class="nav-item dropdown">
            <a class="nav-link dropdown-toggle" href="#" role="button" data-bs-toggle="dropdown" aria-expanded="false">
              Dropdown
            </a>
            <ul class="dropdown-menu">
              {% if is_granted('ROLE_ADMIN') %}
              <li><a class="dropdown-item" href="{{ path ('app_personnel_index') }}">Personnel</a></li>
              <li><a class="dropdown-item" href="{{ path ('app_hospitalisation_index') }}">Hôspitalisation</a></li>
              <li><a class="dropdown-item" href="{{ path ('app_chambre_index') }}">Chambres</a></li>
              {% endif %}
              {% if is_granted('ROLE_PERSONNEL') %}
              <li><a class="dropdown-item" href="{{ path ('app_patient_index') }}">Patient</a></li>
              <li><a class="dropdown-item" href="{{ path ('app_consultation_index') }}">Consultation</a></li>
              <li><a class="dropdown-item" href="{{ path ('app_traitement_index') }}">Traitement</a></li>
              {% endif %}
            </ul>
          </li>
          <li class="nav-item">
            <a class="nav-link" href="{{ path ('app_logout') }}">Déconnexion</a>
          </li>
        </ul>
        {% else %}
        <li class="nav-item">
          <a class="nav-link" href="{{ path ('app_login') }}">Connexion</a>
        </li>
        {% endif %}
      </div>
      <form class="d-flex" role="search">
        <input class="form-control me-2" type="search" placeholder="Search" aria-label="Search">
        <button class="btn btn-outline-success" type="submit">Search</button>
      </form>
    </div>
  </nav>