{% extends 'base.html.twig' %}

{% block title %}Chambre{% endblock %}

{% block body %}
    <h1>Chambre</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ chambre.id }}</td>
            </tr>
            <tr>
                <th>Localis_chamb</th>
                <td>{{ chambre.localisChamb }}</td>
            </tr>
            <tr>
                <th>Type_lit</th>
                <td>{{ chambre.typeLit }}</td>
            </tr>
            <tr>
                <th>N_lit</th>
                <td>{{ chambre.nLit }}</td>
            </tr>
            <tr>
                <th>Prix_chamb</th>
                <td>{{ chambre.prixChamb }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('app_chambre_index') }}">back to list</a>

    <a href="{{ path('app_chambre_edit', {'id': chambre.id}) }}">edit</a>

    {{ include('chambre/_delete_form.html.twig') }}
{% endblock %}
